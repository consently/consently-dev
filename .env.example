# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Widget URL Configuration
# For production: https://www.consently.in
# For development: http://localhost:3000
NEXT_PUBLIC_WIDGET_URL=https://www.consently.in

# Google Translate API
GOOGLE_TRANSLATE_API_KEY=your-google-translate-api-key

# Bhashini API (Optional - for Indian languages)
BHASHINI_USER_ID=your-bhashini-user-id
BHASHINI_API_KEY=your-bhashini-api-key

# Email Configuration
RESEND_API_KEY=your-resend-api-key

# Payment Configuration (Razorpay)
RAZORPAY_KEY_ID=your-razorpay-key-id
RAZORPAY_KEY_SECRET=your-razorpay-key-secret

# Browserless (for cookie scanning)
BROWSERLESS_API_KEY=your-browserless-api-key
BROWSERLESS_URL=https://chrome.browserless.io

# Gemini AI API (for policy generation)
NEXT_PUBLIC_GEMINI_API_KEY=your-gemini-api-key

# Application URL
NEXT_PUBLIC_APP_URL=https://www.consently.in
NEXT_PUBLIC_SITE_URL=https://www.consently.in

# =============================================================================
# DIGILOCKER OAUTH CONFIGURATION (MeriPehchaan/NSSO)
# =============================================================================
# 
# This implementation uses DIRECT DigiLocker MeriPehchaan API for age verification.
# NOT API Setu (which is for document fetching only).
#
# Get credentials from: https://partners.digilocker.gov.in
# 
# ⚠️  CRITICAL SETUP INSTRUCTIONS:
#    1. Create an Auth Partner in DigiLocker Portal with:
#       - Token Authentication Method: authorization_code (NOT client_credentials!)
#       - PKCE: Enabled
#       - Scopes: openid + Profile information (DOB)
#
#    2. Register your redirect URI exactly as configured above
#
#    3. Ensure "Profile information" scope is approved for your partner
#
# ❌ If you get "invalid_client" error, your Auth Partner uses client_credentials
# ✅ Create a NEW Auth Partner with authorization_code grant type
#
# See: docs/DIGILOCKER_AUTH_PARTNER_SETUP.md
# See: docs/DIGILOCKER_IMPLEMENTATION_GUIDE.md

# Environment: 'production' or 'sandbox'
DIGILOCKER_ENV=production

# OAuth Client Credentials (from DigiLocker Partner Portal)
DIGILOCKER_CLIENT_ID=your-digilocker-client-id
DIGILOCKER_CLIENT_SECRET=your-digilocker-client-secret

# Redirect URI (MUST match exactly what's registered in DigiLocker portal)
DIGILOCKER_REDIRECT_URI=https://www.consently.in/api/auth/digilocker/callback

# Issuer ID (your organization's identifier in DigiLocker)
DIGILOCKER_ISSUER_ID=in.consently

# OAuth Scope - MUST include 'profile' for DOB (date of birth) access
# Options:
#   - 'openid profile'  → Returns DOB in id_token or /userinfo (RECOMMENDED)
#   - 'openid'          → Basic info only, may not include DOB
#   - 'openid profile email' → Includes email if available
DIGILOCKER_SCOPE=openid profile

# NSSO ACR (Authentication Context Class Reference)
# Canonical value for MeriPehchaan NSSO integration
# Do not change unless specifically required by DigiLocker team
DIGILOCKER_ACR=digilocker

# =============================================================================
# OPTIONAL: API Setu Configuration (for document fetching features)
# =============================================================================
# NOTE: API Setu is NOT required for basic age verification.
# Only configure if you need to fetch specific issued documents (Aadhaar, PAN, etc.)
# 
# APISETU_BASE_URL=https://apisetu.gov.in/certificate/v3
# APISETU_CLIENT_ID=your-apisetu-client-id
# APISETU_CLIENT_SECRET=your-apisetu-client-secret
